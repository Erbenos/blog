import styled from 'styled-components';

const TitleIcon = styled.span`
  margin-right: 10px;
  margin-left: 4px;

  > svg {
    stroke: var(--cd-news-card-icon-color);
  }

`;

export default TitleIcon;
