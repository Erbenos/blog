import styled from 'styled-components';

const CookiesNote = styled.div`
  color: var(--cd-cookies-note-text-color);
  font-weight: 400;
  line-height: 1.4;
  font-size: 14px;
`;

export default CookiesNote;
